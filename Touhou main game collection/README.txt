1) trong code em có dùng thư viện os để có thể mở đường dẫn liên kết với đối tượng trong listbox được lưu ở trong file games_info_database.txt ạ (button Load và Path), nhưng vì em không gửi mấy tệp exe mà em gán path trong file text lên lms nên có khi ấn nút nó sẽ bị lỗi hoặc dẫn sai đường dẫn hoặc dẫn tới đường dẫn chứa file python ạ

2) và file code thường sẽ hoàn chỉnh hơn file oop ạ, em cảm ơn thầy  

3)from tkinter import *
from tkinter import messagebox
from tkinter import PhotoImage
from tkinter import ttk
from tkinter import filedialog
from tkcalendar import DateEntry
import json
import os

Nguyễn Tiến Quân
21115054120180
